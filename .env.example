# ── Authentication ───────────────────────────────────────────────────────────
# Comma-separated list of valid Bearer tokens (hash them in prod with bcrypt)
API_KEYS=change-me-key-1,change-me-key-2

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated origins allowed to call the API
ALLOWED_ORIGINS=https://sorcery-companion.vercel.app

# ── Rate limiting ─────────────────────────────────────────────────────────────
RATE_LIMIT_PER_MINUTE=60

# ── Model ─────────────────────────────────────────────────────────────────────
CLIP_MODEL=ViT-B-32
CLIP_PRETRAINED=openai

# ── Index paths (populated by generate_embeddings.py) ────────────────────────
EMBEDDINGS_PATH=data/embeddings.npz
INDEX_PATH=data/index.json

# ── Embedding generation (offline use only, NOT loaded by the service) ────────
# DATABASE_URL=postgresql://user:pass@host:5432/db
# R2_PUBLIC_BASE=https://pub-fbad7d695b084411b42bdff03adbffd5.r2.dev/cards
